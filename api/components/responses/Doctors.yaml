description: Successfully retrieved list of doctors.
content:
  application/json:
    schema:
      type: object
      required:
        - doctors
      properties:
        doctors:
          type: array
          items:
            $ref: "../schemas/auth/Doctor.yaml"
