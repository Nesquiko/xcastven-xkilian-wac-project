type: object
properties:
  id:
    type: string
    description: Unique identifier for the illness
  displayName:
    type: string
    description: Human-readable name for the illness
  startDate:
    type: string
    format: date-time
    description: Date when the illness started
  endDate:
    type: string
    format: date-time
    description: Date when the illness ended (if applicable)
    nullable: true
  ended:
    type: boolean
    description: Indicates whether the illness has ended
  appointments:
    type: array
    items:
      $ref: ''
