description: Successfully retrieved list of conditions.
content:
  application/json:
    schema:
      type: object
      required:
        - conditions
      properties:
        conditions:
          type: array
          items:
            $ref: "../schemas/conditions/ConditionDisplay.yaml"
