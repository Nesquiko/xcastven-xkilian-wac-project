name: pageSize
in: query
required: false
description: The number of doctors to return per page.
schema:
  type: integer
  minimum: 1
  maximum: 100 # Set a reasonable maximum page size
  default: 20 # Default page size
example: 50
